[pytest]
log_cli = True
markers =
    disabled: disabled tests
    slow: slow tests
    invite: invites tests
    answer: answers tests
    task: tasks tests
    v2: tests on v2 API version
    broken: Broken tests
