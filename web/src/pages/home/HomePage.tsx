import { Fragment } from 'react';
import AppNavbar from "../../app/navbar/AppNavbar";

const HomePage = () => {
    return (
        <Fragment>
            <AppNavbar />
        </Fragment>
    );
};

export default HomePage;
